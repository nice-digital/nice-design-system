{% if item.return %}
	<div data-g="12 md:6 lg:4">
		<h5>Returns</h5>

		<p>
			{% if item.return.type == '*' %}
				Any type
			{% else %}
				<code>{{ item.return.type | display_as_type }}</code>
			{% endif %}
			{% if item.return.description %} &mdash; {{ item.return.description|striptags }}{% endif %}
		</p>
	</div>
{% endif %}

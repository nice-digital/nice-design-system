{# Describes the usage of @content Sass directive in the documented mixin #}

{% if 'content'|in(item) %}
	<div data-g="12 md:6 lg:4">
		<h5>Content</h5>

		<p>
			This mixin allows extra content to be passed (through the <code>@content</code> directive).
			{% if item.content != "" %}
				Role: {{ item.content }}
			{% endif %}
		</p>
	</div>
{% endif %}
